---
tags:
  - deep-learning
  - exam-prep
---
Related: [[Deep Learning]]

## Optimization

- [x] What is momentum? Write down the formula and explain what $\alpha$ and $\epsilon$ are.
- [x] Difference between momentum and gradient descent
- [x] Vanilla gradient descent — draw the figure
- [x] Adagrad — difference between Adam and Adagrad
- [x] Adam optimization algorithm — besides the concept, know all hyperparameters and what they are for
- [x] For a given network (e.g. discussed in class), which optimization algorithm does it use, and why?

## Regularization

- [ ] Weight decay — formula and explanation -> chiedere ad ago
- [x] L1 regularization
- [x] L2 regularization
- [x] Regularization — general formula and explanation
- [x] Empirical loss — formula and explanation

## Activation Functions

- [x] Activation functions and their diagrams — explain all the ones studied

## Normalization

- [x] Difference between batch normalization and layer normalization — write down the formulas and how $\sigma$ is computed
- [x] Instance normalization

## Convolutional Neural Networks

- [x] Softmax unit
- [x] Convolutional layers — what are the parameters? Which hyperparameters do you need to set when implementing one?
- [x] Inception / GoogLeNet architecture — draw it
- [x] ResNet — draw it
- [x] RCNN, Fast RCNN and Faster RCNN — explanation, plus a brief explanation of Mask RCNN
- [x] How do CNNs work (convolution formula)? 
- [x] How do you design a network for semantic segmentation?

## Recurrent Neural Networks / Sequence Models

- [x] LSTM — what is the structure?
- [ ] Draw the LSTM structure and explain how it captures the same concept as the residual connections in ResNet -> chiedere ad ago
- [x] Difference between vanilla RNN and GRU
- [x] Why and when do we use sigmoid vs. tanh in LSTM, GRU and vanilla RNN?
- [x] RNN samples — give an example
- [ ] PixelRNN
- [x] What have we seen about CNNs in the context of sequence models?

## Autoencoders

- [ ] Difference between autoencoders and variational autoencoders, in terms of latent space

## Generative Adversarial Networks

- [x] Training a GAN — explain and write down the (loss) formula
- [x] Overfitting in GANs
- [ ] Conditional GAN — draw the figure
- [ ] CycleGAN
- [ ] GAN label information
- [x] Why are GANs called "adversarial"?
- [x] How do we measure a GAN? How do we produce a good-quality GAN?
- [x] What are the Inception Score and the Fréchet Inception Distance (FID)?

## Transformers / Attention

- [x] Attention model for image captioning — local vs. global attention, and how to implement them
- [x] Transformer encoder structure and self-attention, explained in detail from input through the computation of self-attention to the output. How can you use a transformer for classifying images?
